apiVersion: v1
kind: Service
metadata:
  name: meridian-web
  labels:
    app: meridian-web
spec:
  type: ClusterIP
  selector:
    app: meridian-web
  ports:
    - name: http
      port: 80
      targetPort: http
